package com.example.myhealth

import android.app.Dialog
import android.content.Context

class ExerciseAddDialog(context: Context) {
    private val dialog = Dialog(context)

    fun myDig(){
        dialog.show()
    }
}